

let person = "Doyin"
let persins = "Oladoyin Daniel"

function sayHello() {
    let person = "Israel"

    return `Hello $`
}  

// This is an object:
const student = {
    name: "Doyin",
    age: 25,
    course: "Computer Science"
};
for (const stud in student) {
    // console.log(stud);
}

// This is an array:
const students = ["Doyin", "Tolu", "Bola", "Sade"];
console.log(persins.trim(4, 6));
console.log(persins);
for (const student of students) {
    console.log(student);
    console.log(students[2])
}

// console.log(indexOf("World"));

function arrTests(){
    const strings = [1, 2, 3, 4 , 5];
    // console.log(strings.splice(1, 3, 9));
    console.log(strings.slice(1, 2))
    console.log(strings);
}

arrTests();

//                      #Assignment from line 81 -86, then 91 and 103.
// Find out if the data mutates or not.
// Find out what they do and what their return values are.