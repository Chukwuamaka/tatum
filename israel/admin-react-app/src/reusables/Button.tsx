// import type { ButtonParams } from "../pages/dashboard/types";

// const Button = ({ imgUrl, text, bgColor, textColor }: ButtonParams) => {
//   return (
//     <button className={`px-4 py-2 flex gap-2 rounded-lg bg-[${bgColor}] `}>
//       <img src={imgUrl} />
//       {text}
//     </button>
//   );
// };

// export default Button;
